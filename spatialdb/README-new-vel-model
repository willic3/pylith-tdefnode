The format of the new velocity model,

#num_lat_grid #num_lon_grid #num_dep_grid
lat_grid
lon_grid
dep_grid
for  ii = 1:lat_grid
  for jj = 1:lon_grid
   vp(ii,jj,1:dep_grid)=fscanf(fid,'%f',dep_grid);
  end
end
for  ii = 1:lat_grid
  for jj = 1:lon_grid
   vs(ii,jj,1:dep_grid)=fscanf(fid,'%f',dep_grid);
  end
end
